<div class="topPart">
    <span>{{ person['pseudo'] }}</span>
</div>
<div class="inboxInfo">
    <div class="view">
        <div class="content">
            <div class="nameDate">
                <div>
                    <span class="categorie">
                        {% if receptorOrSender == 'receptor' %}
                            A :
                        {% else %}
                            De :
                        {% endif %}
                    </span>
                    <span class="name">{{ person['pseudo'] }}</span>
                </div>
            </div>
            <span class="categorie">Objet : </span><span class="titleMsg">{{ object }}</span>
            <div class="viewMessage">
                <p>{{ content|raw }}</p>

            </div>
            <div class="inboxUnderline"></div>
        </div>
    </div>
</div>